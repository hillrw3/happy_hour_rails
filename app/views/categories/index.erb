<br/>
<div class="box">
<h1>Categories</h1>


  <ul>
    <% @categories.each do |category| %>
      <li><%= link_to category.category, category_path(id: category.id) %></li>
    <% end %>
  </ul>
</div>